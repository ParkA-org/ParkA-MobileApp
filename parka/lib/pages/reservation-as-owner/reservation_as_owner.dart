import 'package:flutter/material.dart';

class ReservationAsOwnerPage extends StatelessWidget {
  static String routeName = "reservation-as-owner-page";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text("Reservation as Owner"),
        ),
      ),
    );
  }
}
