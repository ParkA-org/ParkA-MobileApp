import 'package:flutter/material.dart';

class ReservationAsClientPage extends StatelessWidget {
  static String routeName = "reservation-as-client-page";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text("Reservation as Client"),
        ),
      ),
    );
  }
}
